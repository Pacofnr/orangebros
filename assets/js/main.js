const menuBtn=document.querySelector('.menu-btn');const navLinks=document.querySelector('.nav-links');if(menuBtn){menuBtn.addEventListener('click',()=>navLinks.classList.toggle('open'))}document.querySelectorAll('a[href^="#"]').forEach(a=>{a.addEventListener('click',e=>{const id=a.getAttribute('href');if(id.length>1){const el=document.querySelector(id);if(el){e.preventDefault();el.scrollIntoView({behavior:'smooth',block:'start'});navLinks?.classList.remove('open')}}})});const io=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');io.unobserve(entry.target)}})},{threshold:.14,rootMargin:'0px 0px -40px 0px'});document.querySelectorAll('.reveal').forEach((el,i)=>{el.style.transitionDelay=`${Math.min(i%5*.06,.24)}s`;io.observe(el)});window.addEventListener('scroll',()=>{const y=window.scrollY;document.querySelectorAll('.hero-showcase,.page-visual').forEach(el=>{el.style.transform=`translateY(${Math.min(y*.025,18)}px)`})},{passive:true});

const budgetRange=document.getElementById('budgetRange');const budgetValue=document.getElementById('budgetValue');const reachResult=document.getElementById('reachResult');const viewsResult=document.getElementById('viewsResult');const clicksResult=document.getElementById('clicksResult');const followersResult=document.getElementById('followersResult');function fmt(n){return Math.round(n).toLocaleString('en-US')}function updateCalculator(){if(!budgetRange)return;const b=Number(budgetRange.value);budgetValue.textContent=`$${fmt(b)} USD`;reachResult.textContent=`${fmt(b*50)} - ${fmt(b*110)}`;viewsResult.textContent=`${fmt(b*16)} - ${fmt(b*36)}`;clicksResult.textContent=`${fmt(b*.8)} - ${fmt(b*2)}`;followersResult.textContent=`${fmt(b*.16)} - ${fmt(b*.5)}`}budgetRange?.addEventListener('input',updateCalculator);updateCalculator();
